# Focalboard Skill

Open source project management platform.

## Features
- Board view, Table view, Gallery view.
- Traefik integration for SSL.
- SQLite persistence by default.

## Usage
Provide `URL_FOCALBOARD` and `NOME_REDE_INTERNA` to the `run.sh` script.
