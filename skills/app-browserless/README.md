# Browserless Skill

Headless browser utility for automation and scraping.

## Features
- Optimized for performance (Preboot Chrome, Session management).
- Traefik integration for SSL.
- Docker Swarm deployment.

## Usage
Provide `URL_BROWSERLESS` and `NOME_REDE_INTERNA` to the `run.sh` script.
